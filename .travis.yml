notifications:
  email: false

rvm:
  - 2.4.0

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

sudo: false
language: ruby
cache: bundler

script: bundle exec rake ci

after_success:
  coveralls
